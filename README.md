# .p10k.zsh prompt configure file

The ```.p10k.zsh``` file contains the configuration to customize the prompt as seen in the image. The file must be in the following path ```~/.p10k.zsh```.
![plot](shell_ubuntu.png)
